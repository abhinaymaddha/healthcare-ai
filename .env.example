# OpenRouter (primary — all LLM calls route through here)
OPENROUTER_API_KEY=sk-or-v1-your-key-here

# Direct Anthropic (optional — only needed if switching provider to "anthropic")
ANTHROPIC_API_KEY=sk-ant-your-key-here
